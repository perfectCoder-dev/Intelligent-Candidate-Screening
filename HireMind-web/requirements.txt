# Core web framework
flask>=3.0.0
flask-cors>=4.0.0

# Resume parsing
PyPDF2>=3.0.0
python-docx>=1.1.0

# ML / vectorization
scikit-learn>=1.4.0
numpy>=1.26.0

# Deep learning — install CPU-only build unless you have a GPU
# tensorflow-cpu is smaller, faster to install, and identical for inference
tensorflow-cpu>=2.15.0

# Production server
gunicorn>=21.0.0
